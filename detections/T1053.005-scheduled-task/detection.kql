Event
| where Source == "Microsoft-Windows-Sysmon"
| where EventID == 1
| where (EventData has "schtasks.exe" and EventData has "/create") or EventData has_any ("Register-ScheduledTask", "New-ScheduledTask", "RegisterByXml")
| project TimeGenerated, Computer, RenderedDescription
| sort by TimeGenerated desc
